import 'package:flutter/material.dart';

class BookFlightPageMobile extends StatelessWidget {
  const BookFlightPageMobile({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        color: Colors.red,
      ),
    );
  }
}
