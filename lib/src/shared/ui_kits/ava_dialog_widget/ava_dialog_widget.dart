import 'package:flutter/material.dart';

class AvaDialogWidget extends StatelessWidget {
  const AvaDialogWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
